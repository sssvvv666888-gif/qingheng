export const metadata = {
  title: "轻衡 · 减脂生活管理",
  description: "温和记录饮食、运动、步数和日常节奏。"
};

export default function RootLayout({ children }) {
  return <html lang="zh-CN"><body>{children}</body></html>;
}
